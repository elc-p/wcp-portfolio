div.row
  - @posts.each do |post|
      div.col-md-3
        div.card.post-index
          = link_to post_path(post)
            = attachment_image_tag post, :post_image, class: "card-img"
          span id='favorite-#{post.id}'
            == render "favorites/favorite", post: post